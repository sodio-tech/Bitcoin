1. Download or clone this project

2. Open git command prompt and type "cd Bitcoin"

3. In same git command prompt type "npm install" to install node modules.

4. In same git command prompt type "node index.js".

5. You will get "Server connected to port 5000"